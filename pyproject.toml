[project]
name = "gemma3-lo"
version = "0.1.1"
description = "Gemma 3 Impl"
requires-python = ">=3.12"
dependencies = [
    # PyTorch
    "torch",
    "torchvision",

    # Normal Deps
    "numpy>=2.3",
    "pillow>=12.0",
]
